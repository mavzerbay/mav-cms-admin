import { MavDataFromKeyDirective } from './mav-data-from-key.directive';

describe('MavDataFromKeyDirective', () => {
  it('should create an instance', () => {
    const directive = new MavDataFromKeyDirective();
    expect(directive).toBeTruthy();
  });
});
